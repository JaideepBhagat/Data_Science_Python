import numpy as np

scaler = 2

vector = np.array([1, 2, 3])  # Creates a 1D array representing a vector.

matrix = np.array([
    [1,2],
    [3,4]
])  # Creates a 2D array (matrix) with 2 rows and 2 columns.
