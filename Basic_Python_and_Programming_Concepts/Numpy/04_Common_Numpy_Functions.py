import numpy as np

np_array = np.array([1, 2, 3, 4])

# Sum of all elements
total = np.sum(np_array)
print(total)

# Mean of elements
average = np.mean(np_array)
print(average)

# Standard Deviation
std_deviation = np.std(np_array)
print(std_deviation)
